# 自动化测试开发规范

## 执行原则

每个开发批次都遵循：

1. 一次只完成一个编号，例如只做 `M0-1`。
2. 不提前实现后续阶段功能。
3. 完成后报告修改文件、实现内容和剩余事项。
4. 默认不编译、不运行、不启动服务、不执行迁移。
5. 用户确认“进入下一阶段”后才能继续。
6. 遇到数据语义、权限或安全边界不明确时停止并询问。
7. 每个大阶段完成后设置验收关口。

------

## M0：工程骨架

### M0-1 初始化项目目录

实现：

- 初始化 Git 仓库。
- 创建 `backend`、`frontend`、`deploy`、`docs` 目录。
- 创建 `.gitignore`、`.env.example`、`README.md`。
- 只建立必要的顶层结构。

不实现：

- 业务模块。
- 数据库表。
- 登录页面。
- Docker 服务。

完成标准：项目目录和基础配置文件完整。

### M0-2 后端基础骨架

实现：

- 初始化 Python 3.12、uv、FastAPI。
- 建立 `app/main.py`、配置、数据库和公共模块结构。
- `/health` 健康检查。
- 统一成功响应、错误响应和 `request_id`。
- Ruff、mypy、pytest 基础配置。

不实现：认证和具体业务模块。

### M0-3 数据库和迁移基础

实现：

- SQLAlchemy 2 Async。
- asyncpg 数据库连接。
- Alembic 初始化。
- UUID、UTC 时间和基础 Model 约定。
- PostgreSQL 16 + pgvector 的 Docker Compose 配置。

不创建所有业务表，只打好数据库基础。

### M0-4 认证与管理员初始化

实现：

- `User` 模型及迁移。
- Argon2 密码散列。
- JWT Access Token。
- 登录、当前用户接口。
- 管理员初始化机制。
- 用户禁用状态校验。

### M0-5 前端基础骨架

实现：

- Vue 3、Vite、TypeScript strict。
- Vue Router、Pinia、Naive UI、ofetch。
- 全局样式 Token。
- 252px 侧栏、64px 顶栏和管理台布局。
- 紫色状态轨道。
- 登录页和空项目列表页。
- 前端认证状态与路由守卫。

### M0-6 根目录 Agent 规则

此时才根据真实生成的目录、依赖和命令创建根目录 `AGENTS.md`，不能直接复制规划文档里的未验证内容。

M0 验收关口：

- 工程结构符合规范。
- 登录链路代码完整。
- 登录后可进入空项目列表。
- 尚未实现项目业务。

------

## M1：项目、环境和 LLM 配置

### M1-1 项目与项目权限模型

实现：

- `Project`、`ProjectMember`。
- 项目 CRUD。
- 管理员和测试成员的项目级访问校验。
- 项目语言固定为 Java，框架固定为 Spring Boot。

### M1-2 源码来源配置

实现：

- `local_path` 来源登记。
- ZIP 文件上传配置。
- 路径规范化和根目录边界校验。
- Zip Slip 防护。
- 忽略敏感目录、构建目录、二进制和大文件。

本阶段只保存和校验来源，不解析 Java 源码。

### M1-3 OpenAPI 来源配置

实现：

- URL、文件两种来源配置。
- 基础格式和可访问性校验。
- 项目详情中展示配置状态。

本阶段不解析 Path 和 Schema。

### M1-4 测试环境

实现：

- Base URL、环境类型、公共 Header。
- 普通变量和加密变量。
- Host 白名单。
- 是否允许写请求。
- 生产环境禁止执行规则。
- 密钥字段加密存储、不回显。

本阶段不发送真实测试请求。

### M1-5 LLM 配置

实现：

- Provider、Base URL、模型、上下文长度等配置。
- API Key 加密存储。
- 查询接口只返回“已配置”状态。
- 默认配置和启停状态。
- 连接测试接口。

### M1-6 对应前端页面

实现：

- 项目创建和编辑。
- 源码、OpenAPI 配置。
- 环境和变量配置。
- LLM 配置和连接测试。
- API Key 永不回显。

M1 验收关口：能配置项目、环境和 LLM，但不会扫描源码或执行测试接口。

------

## M2：源码扫描与接口发现

### M2-1 后台任务与扫描版本

实现：

- `BackgroundTask`、`SourceScan`、`SourceArtifact`。
- 扫描任务状态机。
- PostgreSQL 单进程 Worker。
- SSE 进度事件。
- 每次扫描独立生成 `scan_version`。

暂不解析具体源码内容。

### M2-2 Java 文件发现与安全读取

实现：

- 只访问项目登记目录。
- 文件过滤和大小限制。
- `.env`、证书、私钥等敏感文件禁止读取。
- `SourceFile` 入库和文件摘要。

### M2-3 Controller 与路由扫描

实现：

- tree-sitter Java AST。
- Controller 类和方法符号。
- Spring 路由注解。
- Path、Method 和参数来源。
- `CodeSymbol`、`ApiDefinition` 基础数据。

### M2-4 DTO、约束和关联符号扫描

实现：

- DTO 字段。
- Bean Validation 约束。
- Service、Repository、Mapper 引用。
- 异常类、错误码和枚举。
- `SymbolRelation`。

### M2-5 OpenAPI 解析与接口合并

实现：

- OpenAPI 3.0、3.1。
- Path、Method、参数、请求体、响应和安全方案。
- 按 Method + 规范化 Path 合并。
- 冲突保留双方数据并标记差异。

### M2-6 接口管理前端

实现：

- 扫描任务和 SSE 进度。
- 模块/包树。
- 接口列表。
- 接口详情抽屉。
- 来源文件、符号和 OpenAPI 差异展示。

M2 验收关口：对 Spring Boot 项目形成稳定接口清单，并能追溯到代码来源。

------

## M3：知识库与混合检索

### M3-1 知识模型与代码切块

实现：

- `KnowledgeChunk`、`BusinessRule` 基础模型。
- 按类、方法、DTO、SQL和异常定义切块。
- 不允许纯固定字符切块。
- 知识记录关联扫描版本。

### M3-2 Embedding 与 pgvector

实现：

- 向量字段和索引迁移。
- Embedding Provider 抽象。
- 批量生成和保存向量。
- 失败状态和重试边界。

### M3-3 精确检索

实现：

- Controller 精确定位。
- DTO、Service、异常调用关系扩展。
- 受控 ripgrep 关键词搜索。
- 项目和路径边界校验。

### M3-4 混合召回

实现：

- 精确结果与向量结果合并。
- 去重和重排。
- 条数和内容长度限制。
- 上下文预算裁剪。
- 返回来源引用和扫描版本。

M3 验收关口：选择接口后能检索到 Controller、DTO、Service、异常等相关上下文。

------

## M4：Agent 分析与用例生成

### M4-1 Agent 数据与运行状态

实现：

- `AgentRun`、`AgentCheckpoint`。
- Prompt 版本管理。
- 模型和参数快照。
- LangGraph 状态定义。
- 运行状态和错误分类。

### M4-2 受控 Agent Tool

分批实现规范中的 Tool：

- 项目和接口读取。
- 代码符号及关系读取。
- 代码搜索和混合检索。
- 已确认规则读取。
- 分析草稿和用例草稿保存。

所有 Tool 必须使用 Pydantic 输入输出，并校验项目权限。

### M4-3 业务规则分析 Graph

实现节点：

- 输入校验。
- 加载接口。
- 检索源码。
- 分析业务规则。
- 保存分析草稿。

必须区分“源码确认”和“AI 推断”。

### M4-4 测试场景生成

实现：

- functional。
- boundary。
- exception。
- permission。
- 每个场景关联业务规则、源码符号和置信度。

本阶段只生成场景，不生成可执行请求。

### M4-5 结构化用例与校验

实现：

- `TestCase`、`TestAssertion` 等模型。
- 结构化请求模板。
- 断言草稿。
- Pydantic 输出校验。
- 最多一次格式修复。
- 禁止生成不存在的接口或字段。

### M4-6 人工审核中断点

实现：

- `draft → pending_review → approved/disabled`。
- LangGraph 人工审核中断和恢复。
- 未审核草稿禁止执行。
- 服务重启后的恢复或明确失败。

### M4-7 AI 分析与用例前端

实现：

- 纵向 Agent 节点轨道。
- 业务规则及来源展示。
- 推断标签和置信度。
- 用例编辑、断言编辑和审核。
- 批量审核，但暂不执行。

M4 验收关口：能生成四类可追溯用例，并必须经过人工审核。

------

## M5：执行器与断言

### M5-1 变量和请求构造

实现：

- `env`、`secret`、`runtime` 变量。
- Path、Query、Header、JSON/Text Body。
- Bearer、Basic、API Key。
- 缺失变量立即失败。

### M5-2 网络安全与 HTTP Runner

实现：

- Host 白名单。
- SSRF 和 DNS Rebinding 防护。
- 生产环境禁止执行。
- 超时、重定向、并发和响应大小限制。
- DNS、连接、TLS、超时错误分类。
- 写请求二次确认所需后端校验。

### M5-3 确定性断言引擎

按小批次实现规范中的十类断言：

- 第一批：状态码、业务码、JSONPath 相等/存在。
- 第二批：JSONPath 类型、文本包含。
- 第三批：数值范围、响应时间。

AI 不参与 Pass/Fail 判定。

### M5-4 执行记录与脱敏

实现：

- `ExecutionRun`、`ExecutionStep`、`AssertionResult`。
- 请求响应快照。
- 耗时、错误和运行变量。
- Token、Cookie、密码、API Key 脱敏。
- 可复现的脱敏 cURL。

### M5-5 单用例和批量执行

实现：

- 已审核用例执行。
- 批量任务。
- 继续执行或停止策略。
- SSE 实时状态。
- 失败用例重新执行。

### M5-6 执行前端

实现：

- 执行确认弹窗。
- 显示环境、Method 和完整目标 Host。
- 实时进度。
- 请求、响应和断言详情。
- 停止任务入口。

M5 验收关口：单接口和批量测试能够确定性执行、断言和留存证据。

------

## M6：关联流程与报告

### M6-1 测试流程模型

实现：

- `TestSuite`、有序步骤。
- 步骤引用已审核用例。
- 请求覆盖。
- `stop_on_failure`。
- 跳过原因记录。

### M6-2 变量提取与下游注入

实现：

- JSONPath。
- 响应头。
- 文本。
- 状态码。
- 变量生产者和消费者校验。
- 运行变量仅在单次执行内有效。

### M6-3 流程执行器

实现：

- 顺序调度。
- 前置失败和变量提取失败分类。
- 登录、创建、查询、修改、删除完整链路。
- 中断后剩余步骤标记跳过。

### M6-4 报告

实现：

- 汇总、通过率和分类统计。
- 接口维度统计。
- 失败原因分组。
- 请求响应、断言和变量证据。
- 脱敏 cURL。
- 失败重试。

### M6-5 失败诊断 Agent

实现独立 Graph：

- 加载执行证据。
- 确定性失败分类。
- 检索相关源码。
- 生成原因假设。
- 保存诊断。

诊断只能提供假设，不能改变最终测试结果。

### M6-6 流程与报告前端

实现：

- 纵向流程步骤列表。
- 步骤排序和变量配置。
- 变量生产者/消费者提示。
- 报告详情和失败重试。
- 不实现复杂拖拽画布。

M6 验收关口：完成“登录 → 创建 → 查询 → 修改 → 删除”的完整回归链路。

------

## M7：安全收口与 MVP 验收

### M7-1 安全专项检查

覆盖：

- 路径越界和 Zip Slip。
- SSRF、DNS Rebinding、Host 白名单。
- 生产环境禁用。
- 密钥、源码、请求响应和日志脱敏。
- 未审核草稿禁止执行。

### M7-2 可追溯性检查

确认报告能追溯：

- 执行版本。
- 用例版本。
- 业务规则。
- 源码符号。
- 扫描版本。
- 模型和 Prompt 版本。

### M7-3 MVP 验收用例

逐项覆盖规范中的：

- AC-001 正常、边界、重复和权限用例。
- AC-002 关联流程。
- AC-003 超时错误隔离。
- AC-004 安全。
- AC-005 可追溯。

### M7-4 部署与交付文档

最后再完善：

- Docker Compose 部署。
- 环境变量说明。
- 初始化管理员说明。
- 源码扫描和测试执行操作说明。
- 根据真实工程更新 `README.md` 和 `AGENTS.md`。