# AI 接口测试 Agent 平台开发需求与实施规范

> 文档用途：供 AI 编程助手和开发人员从零搭建本项目。
>
> 当前阶段：MVP 基础款。
>
> 技术方向：FastAPI + Vue 3 + LangGraph + PostgreSQL/pgvector。
>
> 核心目标：AI 根据 Java/Spring Boot 项目的真实源码和 OpenAPI 生成可审核、可执行、可追溯的 API 测试用例与关联流程。

---

## 1. 项目定位

本项目是一套面向后端 API 的 AI 测试 Agent 平台。用户选择一个接口或一组相关接口后，系统读取相关源码和接口定义，提取参数约束、业务条件、异常分支、权限要求和接口依赖，生成测试用例；用例经用户确认后，由确定性执行器逐条调用测试环境 API，保存请求、响应、断言和错误信息，并生成测试报告。

MVP 首期只支持 Java/Spring Boot REST API 项目。架构需要保留未来扩展其他语言的能力，但不得在首期提前实现多语言适配。

### 1.1 核心价值

系统的核心不是“发送 HTTP 请求”，而是完成以下闭环：

```text
真实源码与 OpenAPI
  → 提取可验证的业务规则
  → 生成有依据的测试场景
  → 转换为结构化测试用例
  → 人工审核
  → 确定性执行与断言
  → 保存证据和回归资产
```

### 1.2 MVP 成功标准

MVP 完成后，用户应能：

1. 创建测试项目并配置源码、OpenAPI 和测试环境。
2. 扫描 Spring Controller 并形成接口清单。
3. 选择一个接口，让 AI 读取 Controller、DTO、Service、异常类等相关源码。
4. 查看 AI 提取的业务规则及其来源文件、类和方法。
5. 生成功能、边界、异常、权限四类测试用例。
6. 审核并修改测试用例、请求参数和断言。
7. 运行单接口用例并保存完整请求、响应、断言和错误。
8. 编排登录、创建、查询、修改、删除等关联接口流程。
9. 从上一步响应中提取变量并传入后续步骤。
10. 查看测试报告并重新执行失败用例。

---

## 2. MVP 范围

### 2.1 必须实现

| 领域 | MVP 能力 |
|---|---|
| 用户与权限 | 管理员登录、JWT 认证、项目级访问校验 |
| 项目管理 | 创建项目、配置源码来源、OpenAPI、测试环境 |
| 源码扫描 | 扫描 Java/Spring Controller、DTO、Service、异常和枚举 |
| 接口发现 | 合并 OpenAPI 与源码扫描结果，形成统一接口定义 |
| 项目知识库 | 保存代码符号、接口、业务规则、向量及扫描版本 |
| Agent | 基于 LangGraph 执行检索、分析、生成、校验、等待审核流程 |
| 用例管理 | 草稿、审核、编辑、启停、分类、来源追溯 |
| API 执行 | HTTP 请求、变量替换、Token 注入、超时和异常处理 |
| 流程测试 | 顺序步骤、变量提取、失败停止或继续 |
| 断言 | 状态码、业务码、JSONPath、文本、类型、范围、响应时间 |
| 执行记录 | 请求响应快照、断言结果、耗时、错误、运行变量 |
| 报告 | 汇总、失败详情、可复制 cURL、失败重试 |
| 系统配置 | LLM Provider、模型、API Key、默认参数 |

### 2.2 首期明确不实现

- UI 浏览器自动化。
- 移动端自动化。
- 性能压测和安全渗透测试。
- AI 自动修改被测项目代码。
- 无人审批地执行写入或删除接口。
- 生产环境自动测试。
- GitHub/GitLab Webhook 自动触发。
- 多租户计费和企业级组织体系。
- 多语言源码分析。
- Kubernetes、Kafka和微服务拆分。
- AI 自主决定任意 Shell 命令或任意网络请求。
- 复杂拖拽式工作流画布。

---

## 3. 用户角色与关键场景

### 3.1 用户角色

MVP 只保留两类角色：

| 角色 | 权限 |
|---|---|
| 管理员 | 用户、项目、LLM、环境、用例、执行和报告全部权限 |
| 测试成员 | 访问授权项目，管理用例并执行测试，不能管理系统密钥和用户 |

### 3.2 核心用户故事

#### US-001 创建项目

用户填写项目名称、Java 源码来源、OpenAPI 地址或文件、测试环境 Base URL，系统校验配置但不自动执行任何业务接口。

#### US-002 扫描接口

用户发起扫描，系统生成扫描任务，解析 Controller、DTO 和 OpenAPI，并在前端实时展示进度。每次扫描产生独立版本，旧用例不能因新扫描而丢失。

#### US-003 分析接口

用户选择一个接口，系统通过精确符号检索、调用关系扩展、关键词搜索和向量搜索构建上下文，Agent 输出业务摘要、规则、异常和关联接口。

#### US-004 生成用例

用户选择需要的测试类型，Agent 生成结构化用例。每条用例必须展示生成依据，不允许只给出没有来源的结论。

#### US-005 审核并执行

用户修改并确认用例后执行。POST、PUT、PATCH、DELETE 在首次执行前必须再次确认当前环境和目标 Host。

#### US-006 关联流程

用户按顺序配置多个已存在的 API 用例，系统从响应中提取 Token、ID 等变量，并注入下游请求。

#### US-007 查看失败

失败报告必须区分网络错误、请求构造错误、断言失败、前置条件失败、变量提取失败和服务端错误，并提供可复现请求。

---

## 4. 总体架构

MVP 采用模块化单体，不拆分多个后端服务。

```text
┌──────────────────── Vue 3 Web ────────────────────┐
│ 项目 / 接口 / AI 分析 / 用例 / 流程 / 执行 / 报告 │
└──────────────── REST + SSE ───────────────────────┘
                         │
┌────────────────── FastAPI Backend ─────────────────┐
│ Platform API                                       │
│  ├─ Auth / Project / Environment / Testcase        │
│ Knowledge Service                                  │
│  ├─ Source Scan / Symbol Index / Hybrid Retrieval  │
│ Agent Runtime                                      │
│  ├─ LangGraph / Prompt / Tool / Checkpoint         │
│ Test Runtime                                       │
│  ├─ HTTP Runner / Variable / Assertion / Redaction │
│ Report Service                                     │
└───────────────────────┬────────────────────────────┘
                        │
             PostgreSQL 16 + pgvector
```

### 4.1 架构原则

1. AI 负责理解、规划、生成和失败解释。
2. HTTP 调用、变量替换和断言由确定性代码执行。
3. Agent 只能通过受控 Tool 访问源码和平台数据。
4. 源码检索采用结构化检索、精确搜索和向量检索的混合方案。
5. 每次分析、生成和执行都必须有持久化任务记录。
6. AI 结果必须经过 Pydantic 校验后才能入库。
7. 用例生成和用例执行分离，未经确认的草稿不能执行。

---

## 5. 技术栈

### 5.1 后端

| 分类 | 技术 | 用途 |
|---|---|---|
| 语言 | Python 3.12 | 后端与 Agent 运行时 |
| Web | FastAPI | REST API、OpenAPI、依赖注入 |
| Server | Uvicorn | ASGI 服务 |
| 配置 | pydantic-settings | 环境配置校验 |
| ORM | SQLAlchemy 2 Async | 异步数据访问 |
| 驱动 | asyncpg | PostgreSQL 异步驱动 |
| 迁移 | Alembic | 数据库版本迁移 |
| 数据库 | PostgreSQL 16 | 业务数据、任务、执行记录 |
| 向量 | pgvector | 代码和业务语义向量 |
| Agent | LangGraph Python | 有状态工作流、暂停、恢复、人工审核 |
| LLM 抽象 | langchain-core | 消息、模型和 Tool 基础协议 |
| 模型适配 | langchain-openai | OpenAI 兼容模型接入 |
| 原生 SDK | openai | 需要直接控制结构化输出时使用 |
| HTTP | HTTPX | API 测试执行和外部模型请求 |
| 源码解析 | tree-sitter、tree-sitter-java | Java AST 和符号提取 |
| 精确搜索 | ripgrep | 类名、方法名、异常码和引用搜索 |
| 认证 | PyJWT、pwdlib[argon2] | JWT 和密码散列 |
| 加密 | cryptography | LLM Key、测试密钥加密 |
| 测试 | pytest、pytest-asyncio | 后端测试 |
| 规范 | Ruff、mypy | 格式、静态检查和类型检查 |

### 5.2 前端

| 分类 | 技术 | 用途 |
|---|---|---|
| 语言 | TypeScript | 前端类型安全 |
| 框架 | Vue 3 Composition API | 页面与组件 |
| 构建 | Vite | 开发和构建 |
| 路由 | Vue Router | 页面路由和权限守卫 |
| 状态 | Pinia | 用户、项目、任务状态 |
| UI | Naive UI | 表格、表单、弹窗、树、标签 |
| HTTP | ofetch | REST API 封装 |
| 校验 | Zod | 表单和动态 JSON 校验 |
| 图标 | lucide-vue-next | 统一线性图标 |
| 时间 | dayjs | 时间格式化 |
| Markdown | marked + DOMPurify | AI 内容安全渲染 |
| API 类型 | openapi-typescript | 从 FastAPI OpenAPI 生成类型 |
| 测试 | Vitest、Vue Test Utils | 组件与逻辑测试 |
| E2E | Playwright | 后续用于平台自身关键流程测试 |
| 规范 | ESLint、Prettier、vue-tsc | 静态检查和类型检查 |

### 5.3 部署

| 技术 | 用途 |
|---|---|
| Docker Compose | 编排 frontend、backend、postgres |
| Nginx | 静态页面、API 反代、SSE 连接 |
| uv | Python 依赖和虚拟环境 |
| pnpm | 前端包管理 |

MVP 不引入 Redis。后台任务先使用 PostgreSQL 任务表、LangGraph Checkpoint 和单进程 Worker。需要多实例并发时，再评估 Redis/Celery 或独立 Worker。

---

## 6. 从零搭建步骤

以下命令是项目初始化规划，实际执行前由开发者确认本机环境。

### 6.1 环境要求

- Git。
- Python 3.12。
- uv。
- Node.js 22 LTS。
- pnpm。
- Docker Desktop 或 Docker Engine + Compose v2。
- ripgrep。

### 6.2 初始化目录

```text
ai-api-test-platform/
├── backend/
├── frontend/
├── deploy/
├── docs/
├── .env.example
├── docker-compose.yml
├── README.md
└── AGENTS.md
```

### 6.3 初始化后端

规划命令：

```bash
mkdir backend
cd backend
uv init --python 3.12
uv add fastapi "uvicorn[standard]" pydantic-settings sqlalchemy asyncpg alembic
uv add pgvector httpx openai langgraph langchain-core langchain-openai
uv add tree-sitter tree-sitter-java python-multipart pyjwt "pwdlib[argon2]" cryptography
uv add --dev pytest pytest-asyncio ruff mypy
```

后端初始目录：

```text
backend/
├── app/
│   ├── main.py
│   ├── config.py
│   ├── database.py
│   ├── common/
│   │   ├── errors.py
│   │   ├── responses.py
│   │   ├── security.py
│   │   └── redaction.py
│   ├── modules/
│   │   ├── auth/
│   │   ├── users/
│   │   ├── projects/
│   │   ├── environments/
│   │   ├── source_scans/
│   │   ├── api_definitions/
│   │   ├── knowledge/
│   │   ├── agents/
│   │   ├── testcases/
│   │   ├── test_suites/
│   │   ├── executions/
│   │   ├── assertions/
│   │   ├── reports/
│   │   └── llm_configs/
│   └── workers/
│       ├── task_worker.py
│       └── task_dispatcher.py
├── alembic/
├── tests/
└── pyproject.toml
```

每个业务模块统一使用：

```text
module/
├── models.py
├── schemas.py
├── repository.py
├── service.py
└── router.py
```

纯算法模块可省略不需要的层，不允许为了形式创建空文件。

### 6.4 初始化前端

规划命令：

```bash
pnpm create vite frontend --template vue-ts
cd frontend
pnpm add vue-router pinia naive-ui ofetch zod lucide-vue-next dayjs marked dompurify
pnpm add -D @types/dompurify openapi-typescript vitest @vue/test-utils playwright
```

前端初始目录：

```text
frontend/src/
├── api/
├── assets/
├── components/
├── composables/
├── layouts/
├── router/
├── stores/
├── styles/
│   ├── tokens.css
│   ├── reset.css
│   └── global.css
├── types/
│   ├── api.generated.ts
│   └── domain.ts
└── views/
    ├── auth/
    ├── dashboard/
    ├── projects/
    ├── api-definitions/
    ├── analysis/
    ├── testcases/
    ├── test-suites/
    ├── executions/
    ├── reports/
    └── settings/
```

`api.generated.ts` 由 OpenAPI 自动生成，不允许手工修改。

### 6.5 初始化数据库和部署

Docker Compose 首期包含：

```text
postgres
backend
frontend
```

PostgreSQL 镜像需要启用 pgvector。开发环境可只启动 PostgreSQL，前后端在本机启动；生产环境统一使用容器。

环境变量至少包括：

```dotenv
APP_ENV=development
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/ai_test
JWT_SECRET=
ENCRYPTION_KEY=
FRONTEND_ORIGIN=http://localhost:5173
SOURCE_ROOT_ALLOWLIST=
DEFAULT_REQUEST_TIMEOUT_SECONDS=20
MAX_RESPONSE_BODY_BYTES=1048576
```

真实密钥只放 `.env`，仓库只提交 `.env.example`。

---

## 7. 后端业务模块需求

### 7.1 Auth 与用户

- 用户名和密码登录。
- JWT Access Token。
- 密码使用 Argon2 散列。
- 管理员创建和禁用用户。
- 所有项目资源接口必须校验项目访问权限。

### 7.2 项目管理

项目字段至少包含：

```text
id
name
description
source_type: local_path | zip_upload
source_location
openapi_source_type: url | file
openapi_location
default_environment_id
language: java
framework: spring_boot
created_by
created_at
updated_at
```

MVP 源码输入支持：

1. 本地部署时登记服务端可访问目录。
2. 上传 ZIP 源码包。

ZIP 解压必须防止 Zip Slip，源码访问必须限制在项目登记根目录内。Git 仓库拉取放到后续版本。

### 7.3 测试环境

环境包含：

- 名称。
- Base URL。
- 环境类型：测试、预发、生产。
- 公共 Header。
- 普通变量。
- 加密变量。
- 是否允许写请求。
- Host 白名单。

生产环境默认禁止执行。MVP 不提供绕过开关。

### 7.4 源码扫描

首期识别以下 Spring 注解：

```text
@RestController
@RequestMapping
@GetMapping
@PostMapping
@PutMapping
@PatchMapping
@DeleteMapping
@RequestBody
@RequestParam
@PathVariable
@RequestHeader
@Valid
@Validated
```

识别以下 Bean Validation 规则：

```text
@NotNull
@NotBlank
@NotEmpty
@Size
@Min
@Max
@DecimalMin
@DecimalMax
@Positive
@PositiveOrZero
@Negative
@NegativeOrZero
@Pattern
@Email
```

扫描输出：

- 文件记录。
- 类、方法、字段等代码符号。
- Controller 路由。
- DTO 字段约束。
- Service 方法引用。
- Repository/Mapper 引用。
- 异常类和错误码。
- 枚举值。
- 符号之间的调用或引用关系。

每次扫描必须生成 `scan_version`，知识记录与扫描版本关联。

### 7.5 OpenAPI 解析

- 支持 OpenAPI 3.0 和 3.1。
- 支持 URL 和文件导入。
- 解析 Path、Method、operationId、参数、请求体、响应和安全方案。
- OpenAPI 与源码路由按 Method + 规范化 Path 合并。
- 冲突时保留双方数据并标记差异，不能静默覆盖。

### 7.6 项目知识库

源码知识库不是单纯向量库，必须同时包含结构化索引和向量索引。

#### 结构化索引

```text
source_files
code_symbols
symbol_relations
api_definitions
business_rules
scan_versions
```

#### 向量内容

- 类摘要。
- 方法代码和摘要。
- DTO 及其约束。
- 异常定义。
- 接口说明。
- 已确认的业务规则。
- 项目文档。

代码切块必须按类、方法、DTO、SQL 或异常定义切分，不允许只按固定字符数切分。

#### 混合检索顺序

```text
接口 → Controller 符号
     → Service/DTO/异常精确扩展
     → ripgrep 关键词补充
     → pgvector 语义召回
     → 去重和重排
     → 上下文预算裁剪
```

向量检索不能替代精确符号检索。

### 7.7 LLM 配置

字段至少包含：

```text
name
provider
base_url
model
encrypted_api_key
context_window
temperature
max_output_tokens
is_default
enabled
```

要求：

- 兼容 OpenAI 协议。
- 支持连接测试。
- API Key 加密存储，读取接口只返回“已配置”。
- 日志、异常和前端响应不得包含完整 Key。
- 每次 Agent 运行记录实际模型和参数快照。

### 7.8 测试用例

测试分类：

```text
functional
boundary
exception
permission
workflow
```

测试状态：

```text
draft
pending_review
approved
disabled
```

每条用例至少包含：

- 名称和说明。
- 分类和优先级。
- 关联接口。
- 前置条件。
- 请求模板。
- 断言列表。
- 变量提取规则。
- 清理说明。
- 来源业务规则。
- 来源文件和符号。
- AI 置信度。
- 人工审核状态。

### 7.9 API 执行器

执行器支持：

- GET、POST、PUT、PATCH、DELETE。
- Path、Query、Header、JSON Body、Text Body。
- Bearer、Basic、API Key。
- 登录接口提取 Token。
- 环境变量和运行变量替换。
- 超时、DNS、连接和 TLS 错误分类。
- 响应体大小限制。
- 重定向策略配置。
- 单接口、批量和顺序流程执行。

变量语法：

```text
{{env.baseUrl}}
{{env.username}}
{{secret.password}}
{{runtime.token}}
{{runtime.userId}}
```

未找到变量时必须失败，不允许替换为空字符串后继续。

### 7.10 断言引擎

MVP 支持：

```text
status_code_equals
business_code_equals
json_path_equals
json_path_exists
json_path_not_exists
json_path_type
body_contains
body_not_contains
number_range
response_time_less_than
```

断言结果必须保存：

```text
type
path
expected
actual
passed
message
```

AI 不参与最终 Pass/Fail 判定。

### 7.11 流程执行

- 流程由有序步骤组成。
- 每一步引用一个已审核用例。
- 支持步骤级请求覆盖。
- 支持从 JSONPath、响应头、文本或状态码提取变量。
- 支持 `stop_on_failure`。
- 被跳过步骤必须记录跳过原因。
- 流程运行时变量只在本次运行中有效。

### 7.12 报告

报告必须包含：

- 项目、环境、扫描版本、用例版本。
- 总数、通过、失败、跳过和通过率。
- 分类和接口维度统计。
- 每步请求响应快照。
- 断言期望值和实际值。
- 网络或执行错误。
- 变量提取结果。
- 可复制的脱敏 cURL。
- 重新执行失败用例入口。

---

## 8. Agent 与 LangGraph 设计

### 8.1 Agent 边界

Agent 可以：

- 检索项目源码和结构化索引。
- 获取接口定义和相关业务规则。
- 总结业务逻辑。
- 生成测试场景和草稿用例。
- 校验用例完整性。
- 分析失败证据并给出原因假设。

Agent 不可以：

- 读取项目根目录以外的文件。
- 执行 Shell 命令。
- 直接操作数据库。
- 直接调用任意 URL。
- 自动执行未审核用例。
- 修改被测项目源码。
- 将密钥发送给模型。

### 8.2 Agent Tool

首期只提供受控 Tool：

```text
get_project_summary
get_api_definition
get_code_symbol
get_symbol_relations
search_code
retrieve_related_context
get_confirmed_business_rules
save_analysis_draft
save_testcase_drafts
```

每个 Tool 必须：

- 使用 Pydantic 定义输入输出。
- 校验项目权限和路径边界。
- 限制结果条数和内容长度。
- 记录调用名称、耗时和结果摘要。
- 不在日志中输出源码全文和密钥。

### 8.3 LangGraph 节点

```text
START
  → validate_input
  → load_api_definition
  → retrieve_code_context
  → analyze_business_rules
  → generate_test_scenarios
  → generate_structured_cases
  → validate_generated_cases
  → persist_drafts
  → human_review_interrupt
  → END
```

失败分析使用独立 Graph：

```text
START
  → load_execution_evidence
  → classify_failure
  → retrieve_related_code
  → generate_diagnosis
  → persist_diagnosis
  → END
```

### 8.4 Agent State

```python
class TestGenerationState(TypedDict):
    project_id: str
    api_ids: list[str]
    scan_version_id: str
    api_definitions: list[dict]
    retrieved_context: list[dict]
    business_rules: list[dict]
    scenarios: list[dict]
    generated_cases: list[dict]
    validation_errors: list[str]
    review_status: str
    model_snapshot: dict
```

状态只保存结构化摘要和引用，不保存无限增长的完整消息历史。

### 8.5 AI 输出约束

- 所有核心输出必须使用 Pydantic 模型校验。
- 模型输出失败时只允许一次格式修复。
- 第二次仍失败则任务标记失败并保存脱敏原始输出。
- 业务规则必须包含来源；没有来源时标记为 `inferred`。
- 推断规则默认低置信度，不得自动成为强断言。
- 测试用例不得生成不存在的接口或字段。

---

## 9. 核心数据模型

MVP 主要实体：

```text
User
Project
ProjectMember
SourceArtifact
SourceScan
SourceFile
CodeSymbol
SymbolRelation
ApiDefinition
BusinessRule
KnowledgeChunk
TestEnvironment
EnvironmentVariable
LlmConfig
AgentRun
AgentCheckpoint
TestSuite
TestCase
TestStep
TestAssertion
ExecutionRun
ExecutionStep
AssertionResult
BackgroundTask
```

关系概览：

```text
Project
├── SourceScan
│   ├── SourceFile
│   ├── CodeSymbol
│   ├── SymbolRelation
│   ├── ApiDefinition
│   └── KnowledgeChunk
├── BusinessRule
├── TestEnvironment
├── AgentRun
├── TestSuite
│   └── TestCase
│       ├── TestStep
│       └── TestAssertion
└── ExecutionRun
    └── ExecutionStep
        └── AssertionResult
```

请求模板、响应快照、AI 分析结果等不稳定结构使用 PostgreSQL JSONB；项目、接口、用例和执行记录等核心关系使用正常字段和外键。

---

## 10. API 设计约定

统一前缀：

```text
/api/v1
```

核心路由：

```text
POST   /auth/login
GET    /auth/me

GET    /projects
POST   /projects
GET    /projects/{project_id}
PATCH  /projects/{project_id}

POST   /projects/{project_id}/source-scans
GET    /projects/{project_id}/source-scans/{scan_id}
GET    /projects/{project_id}/source-scans/{scan_id}/events

GET    /projects/{project_id}/apis
GET    /projects/{project_id}/apis/{api_id}
POST   /projects/{project_id}/apis/analyze

POST   /projects/{project_id}/agent-runs/testcase-generation
GET    /projects/{project_id}/agent-runs/{run_id}
GET    /projects/{project_id}/agent-runs/{run_id}/events
POST   /projects/{project_id}/agent-runs/{run_id}/approve

GET    /projects/{project_id}/testcases
POST   /projects/{project_id}/testcases
PATCH  /projects/{project_id}/testcases/{case_id}

GET    /projects/{project_id}/test-suites
POST   /projects/{project_id}/test-suites
POST   /projects/{project_id}/test-suites/{suite_id}/run

POST   /projects/{project_id}/testcases/{case_id}/run
GET    /projects/{project_id}/executions/{run_id}
GET    /projects/{project_id}/executions/{run_id}/events

GET    /projects/{project_id}/reports
GET    /projects/{project_id}/reports/{run_id}

GET    /llm-configs
POST   /llm-configs
POST   /llm-configs/{config_id}/test
```

响应格式：

```json
{
  "data": {},
  "message": "操作成功",
  "requestId": "uuid"
}
```

错误格式：

```json
{
  "code": "TEST_VARIABLE_MISSING",
  "message": "运行变量 userId 不存在",
  "details": {},
  "requestId": "uuid"
}
```

错误码必须稳定，前端不能依赖中文错误文本判断逻辑。

---

## 11. 前端信息架构

### 11.1 主导航

```text
概览
项目管理
接口管理
AI 分析
用例管理
测试流程
执行记录
测试报告
系统设置
  ├─ LLM 配置
  └─ 用户管理
```

首期不展示“需求管理、测试物料、UI 自动化、通用 AI 对话”等未实现入口。

### 11.2 页面需求

#### 概览

- 当前项目卡片。
- 接口数量、已审核用例、最近执行、通过率。
- 最近 Agent 任务和测试任务。
- 最近失败摘要。

#### 项目管理

- 项目列表和创建入口。
- 源码来源、OpenAPI 和环境状态。
- 最近扫描时间和扫描结果。

#### 接口管理

- 左侧模块/包树。
- 右侧接口表格。
- Method、Path、Controller、规则数量、用例数量。
- 接口详情抽屉展示请求、响应、源码来源、关联符号和规则。

#### AI 分析

- 选择一个或多个接口。
- 显示 Graph 节点进度。
- 展示业务摘要、参数规则、异常、权限和关联接口。
- 每条规则可跳转到来源文件和符号。
- 提供“生成测试用例”操作。

#### 用例管理

- 按接口和类型筛选。
- 用例状态、优先级和来源标记。
- 请求编辑、断言编辑、变量提取配置。
- 批量审核、启用和执行。

#### 测试流程

- 使用纵向步骤列表，不做拖拽画布。
- 可添加步骤、调整顺序、配置提取变量和失败策略。
- 实时提示变量生产者和消费者。

#### 执行记录

- SSE 实时进度。
- 当前步骤、完成数、通过、失败和跳过。
- 请求响应详情和断言列表。
- 支持停止当前任务，但不承诺终止已经发出的 HTTP 请求。

#### 测试报告

- 汇总指标。
- 分类和接口维度结果。
- 失败原因分组。
- 请求响应和脱敏 cURL。
- 失败重试入口。

#### LLM 配置

- 表格展示名称、Provider、模型、Key 状态、上下文长度和操作。
- 支持新增、测试连接、编辑、删除和设置默认。
- API Key 永不回显。

---

## 12. UI 视觉规范

本节基于提供的三张界面参考图提炼。目标是延续清爽、专业、低噪声的 AI 测试管理台风格，同时为源码分析和执行状态建立更明确的视觉识别。

### 12.1 设计方向

视觉关键词：

```text
冷静
可信
轻量
技术感
低噪声
高信息密度
紫蓝色状态导向
```

视觉标志性元素为“紫色状态轨道”：当前导航、当前 Agent 节点、选中卡片和报告重点均使用左侧 3px 紫色轨道或紫色细描边表达状态。紫色只承担“当前、智能、主要操作”含义，不能大面积滥用。

### 12.2 色彩 Token

```css
:root {
  --color-primary: #5548e8;
  --color-primary-hover: #493cd8;
  --color-primary-soft: #e9e8ff;
  --color-primary-softer: #f3f2ff;

  --color-page: #f5f6fb;
  --color-surface: #ffffff;
  --color-surface-muted: #f8f9fc;
  --color-border: #e6e8f0;

  --color-text: #182033;
  --color-text-secondary: #526078;
  --color-text-muted: #9aa6bd;

  --color-info: #22a7f0;
  --color-info-soft: #e5f5ff;
  --color-success: #39b86a;
  --color-success-soft: #e5f7eb;
  --color-warning: #e6a23c;
  --color-warning-soft: #fff4dc;
  --color-danger: #ff4d5a;
  --color-danger-soft: #ffecef;
}
```

规则：

- 页面背景使用冷灰白，不使用纯白铺满整个工作区。
- 白色只用于侧栏、顶部栏、表格、卡片和编辑面板。
- 主按钮使用纯紫色，禁止默认渐变。
- 成功、警告和错误只用于真实状态，不作为装饰色。
- 大段代码和 JSON 使用中性背景，避免紫色背景影响阅读。

### 12.3 字体

```css
font-family:
  Inter,
  "Noto Sans SC",
  "PingFang SC",
  "Microsoft YaHei",
  sans-serif;
```

数据、状态码、耗时、Method 和代码路径使用：

```css
font-family: "JetBrains Mono", "SFMono-Regular", Consolas, monospace;
```

字号体系：

| 用途 | 字号 | 字重 |
|---|---:|---:|
| 页面标题 | 24px | 650 |
| 区块标题 | 18px | 600 |
| 卡片标题 | 16px | 600 |
| 正文/表格 | 14px | 400/500 |
| 辅助说明 | 13px | 400 |
| 标签/时间 | 12px | 500 |

中文标题不得使用过度粗重的 800/900 字重。

### 12.4 布局

```text
┌──── 252px 侧栏 ────┬──────────── 64px 顶栏 ────────────┐
│ Logo               │ 当前项目 / 面包屑       用户菜单   │
│                    ├─────────────────────────────────────┤
│ 一级导航           │ 页面标题                  主操作    │
│                    │ 页面说明                            │
│ 当前项带紫色轨道   │                                     │
│                    │ 内容卡片 / 表格 / 工作区            │
└────────────────────┴─────────────────────────────────────┘
```

- 桌面侧栏宽度：252px。
- 顶栏高度：64px。
- 内容区最大宽度不强制居中；管理台优先利用横向空间。
- 页面内边距：32px，大屏可到 36px。
- 区块间距：24px。
- 表单项间距：20px。
- 卡片内边距：20px 或 24px。
- 页面最小适配宽度：1280px。
- 低于 1280px 时侧栏收起为 72px 图标栏。
- 不将核心数据表格强行适配手机；移动端仅提供查看和任务状态。

### 12.5 圆角和阴影

```css
--radius-sm: 6px;
--radius-md: 10px;
--radius-lg: 14px;
--shadow-card: 0 1px 2px rgba(25, 32, 51, 0.04),
               0 8px 24px rgba(25, 32, 51, 0.035);
```

- 按钮和输入框：8px。
- 卡片和弹窗：12px 至 14px。
- 表格容器：14px。
- 阴影保持极淡，层级优先由背景和边框表达。
- 禁止所有元素都使用悬浮阴影。

### 12.6 导航

- 图标使用 20px、1.75px 线宽的线性图标。
- 一级菜单高度 48px。
- 当前项使用浅紫背景、紫色文字和左侧 3px 状态轨道。
- 二级菜单缩进 28px，不重复显示父级图标。
- 展开和收起状态使用统一 Chevron。
- Logo 区高度与顶部栏视觉对齐。

### 12.7 按钮和操作

- 页面只能有一个主要按钮。
- 主按钮高度 40px，紫色背景，白色文字。
- 次要按钮使用白底描边。
- 表格行内操作使用文字按钮：查看/测试为中性色，编辑为信息蓝，删除为危险红。
- 危险操作必须二次确认。
- 图标按钮必须提供 Tooltip 和可访问名称。

### 12.8 表格

- 表头使用非常浅的紫灰背景。
- 表头文字 13px/600，正文 14px。
- 行高 64px，复杂内容可使用 72px。
- 行分隔线使用 `--color-border`。
- 状态使用低饱和度胶囊标签，不使用纯色大色块。
- 长 Path、模型名和源码路径使用省略号，悬停显示完整内容。
- 空表格必须提供下一步操作，例如“尚未扫描接口，开始扫描项目”。

### 12.9 卡片

- 卡片为白底、浅边框、14px 圆角。
- 选中卡片使用紫色细边框和轻微紫色外发光，不改变整体尺寸。
- 卡片顶部为图标、标题和状态标签。
- 描述最多显示两行。
- 底部使用分隔线展示更新时间和次要操作。

### 12.10 Agent 运行界面

Agent 进度采用纵向节点轨道，而不是普通聊天气泡堆叠：

```text
● 读取接口定义       已完成  0.4s
│
● 检索相关源码       已完成  1.8s
│
◉ 分析业务规则       执行中
│
○ 生成测试场景       等待中
│
○ 生成结构化用例     等待中
```

- 已完成：绿色圆点和中性文字。
- 执行中：紫色脉冲圆环，只在此处使用持续动画。
- 失败：红色圆点和明确错误原因。
- 等待：灰色空心圆。
- 遵循 `prefers-reduced-motion`，关闭脉冲动画后仍能识别状态。

### 12.11 AI 分析内容

- AI 输出使用文档卡片，不使用左右聊天气泡。
- 业务规则用可折叠列表展示。
- 来源文件、类、方法使用等宽字体和可复制标签。
- 推断内容必须显示“AI 推断”标签。
- 已从源码确认的规则显示“源码确认”标签。
- 关键结论可使用左侧紫色轨道强调，禁止整块高饱和背景。

### 12.12 交互与文案

- 按钮使用用户能理解的动作名称：`扫描项目`、`生成用例`、`开始执行`、`保存修改`。
- 禁止使用只有技术实现含义的名称，例如“触发 Graph”或“调用 Worker”。
- 加载状态说明当前阶段，不只显示“处理中”。
- 错误信息说明发生了什么和如何处理。
- 空状态提供一个明确主动作。
- Toast 文案与按钮动作保持一致。

### 12.13 可访问性

- 所有交互元素必须支持键盘操作。
- 焦点样式使用 2px 紫色外轮廓，不得移除默认焦点后不补充。
- 正文与背景对比度符合 WCAG AA。
- 颜色不能作为唯一状态表达，必须同时使用文字或图标。
- 所有表单错误与字段关联。

---

## 13. 安全要求

### 13.1 源码安全

- 项目只能访问登记目录以内的文件。
- 对所有路径执行规范化和父目录校验。
- 忽略 `.git`、`node_modules`、`target`、`dist`、二进制和大文件。
- 禁止 Agent 直接读取 `.env`、证书、私钥和密钥文件。
- 源码默认不写入普通日志。

### 13.2 网络安全

- 测试环境必须配置 Host 白名单。
- 禁止访问云元数据地址和平台自身管理地址。
- DNS 解析后再次校验目标 IP，防止 SSRF 和 DNS Rebinding。
- 默认禁止生产环境。
- 写请求执行前显示环境、Method 和完整目标 Host。
- 限制超时、并发数、重定向次数和响应大小。

### 13.3 敏感信息

请求、响应、日志、报告和 cURL 必须脱敏：

```text
Authorization
Cookie
Set-Cookie
password
token
accessToken
refreshToken
secret
apiKey
```

环境变量需要区分普通变量和加密变量。加密变量的值只在执行器内短暂解密，不传给 LLM。

---

## 14. 开发规范

### 14.1 通用规范

- 回复和产品文案使用中文。
- 代码注释使用中文，只解释关键业务原因。
- 修改前先理解现有代码意图和调用链。
- 只修改当前需求涉及的代码，不顺带重构无关模块。
- 不引入没有必要的依赖。
- 不创建没有实际用途的抽象、文件或目录。
- 环境配置不得硬编码。
- 所有时间在数据库中使用 UTC，前端按用户时区显示。
- 所有 ID 使用 UUID。
- 所有重要状态使用枚举，不使用散落的字符串。

### 14.2 Python 规范

- 使用 Python 3.12 类型注解。
- 对外函数必须声明参数和返回类型。
- 请求、响应和 Agent 输出统一使用 Pydantic Model。
- 数据访问只放 Repository，业务事务放 Service。
- Router 不写业务逻辑。
- 不使用裸 `except Exception` 吞掉异常；任务边界可捕获，但必须分类、记录并更新任务状态。
- 异步路径中禁止使用阻塞 I/O；源码解析等 CPU 工作放入线程或 Worker。
- 不返回 SQLAlchemy Model，统一转换为 Response Schema。
- Ruff 行宽统一为 120。

命名：

```text
文件、变量、函数：snake_case
类和 Pydantic Model：PascalCase
常量：UPPER_SNAKE_CASE
```

### 14.3 TypeScript/Vue 规范

- 使用 TypeScript strict 模式。
- 组件使用 PascalCase，函数和变量使用 camelCase。
- 使用 `<script setup lang="ts">` 和 Composition API。
- 不使用 `any`；未知外部数据使用 `unknown` 并校验。
- 页面只负责组合，业务逻辑放 composable 或 store。
- API 请求集中放在 `src/api`。
- Pinia 只保存跨页面状态，局部状态保留在组件。
- 不直接修改自动生成的 OpenAPI 类型。
- 所有可复用颜色、间距、圆角使用 Token。
- 不在组件内散落硬编码颜色。

### 14.4 数据库规范

- 每次表结构变更必须新增 Alembic Migration。
- Migration 不允许依赖当前 ORM Model 的运行时状态。
- 外键删除策略必须显式声明。
- 常用查询字段建立索引。
- JSONB 只用于结构不稳定的数据，不替代正常关系设计。
- 执行记录不可因用例被删除而失去历史快照。

### 14.5 Agent 规范

- Prompt 纳入版本管理。
- 每次运行保存 Prompt 版本、模型和参数快照。
- Tool 输入输出必须有 Schema。
- 生成结果必须可追溯到检索上下文。
- 不将完整仓库一次性发送给模型。
- 不允许模型直接决定最终断言结果。
- 业务推断与源码事实必须分开标识。
- 低置信度规则必须进入人工审核。

### 14.6 日志和错误

- 每个请求生成 `request_id`。
- 每个后台任务生成 `task_id`。
- 日志记录事件、对象 ID、阶段、耗时和错误码。
- 不记录完整 Token、密码、API Key、Cookie 和大段源码。
- 用户可处理的错误使用稳定业务错误码。
- 未知异常统一转换为内部错误，详细堆栈只进入服务端受保护日志。

---

## 15. AI 编程助手执行规则

AI 开发本项目时必须遵守：

1. 开始任务前阅读本文件和根目录 `AGENTS.md`。
2. 先检查当前工作区，不能假设规划中的文件已经存在。
3. 每次只完成一个明确里程碑，不跨阶段提前实现功能。
4. 修改前追踪真实调用链和数据所有者。
5. 保持最小修改，不优化无关代码。
6. 不能自行改变产品范围、数据语义或安全边界。
7. 需求存在业务歧义时停止修改并询问用户。
8. 新依赖必须说明用途，并确认现有能力无法满足。
9. 不自动运行编译、测试、迁移或启动服务；除非用户在当次任务明确要求。
10. 可以编写测试代码，但不得在未授权时执行。
11. 不创建演示数据、临时脚本和无关文档。
12. 完成后报告修改文件、实现内容、未执行的验证和剩余风险。

项目实际初始化后，应根据真实目录、命令和 CI 配置生成根目录 `AGENTS.md`；不得把本规划中的命令当作已验证事实复制进去。

---

## 16. 开发里程碑

### M0：工程骨架

- FastAPI、Vue、PostgreSQL 和 Docker Compose 骨架。
- 基础配置、健康检查、统一响应和错误。
- 登录和管理员初始化。
- 前端管理台布局、路由和主题 Token。

验收：能够登录并看到空项目列表；前后端健康检查正常。

### M1：项目、环境与 LLM

- 项目 CRUD。
- 源码目录/ZIP 配置。
- OpenAPI 配置。
- 测试环境和变量。
- LLM 配置、加密和连接测试。

验收：能创建项目、环境和 LLM 配置，Key 不回显。

### M2：源码扫描与接口发现

- Spring Controller 和 DTO 扫描。
- OpenAPI 导入。
- 接口合并。
- 符号、关系和扫描版本入库。
- SSE 扫描进度。

验收：对示例 Spring 项目生成稳定接口清单，并显示来源代码。

### M3：知识库与混合检索

- pgvector。
- 代码切块和 Embedding。
- 精确符号检索。
- 调用关系扩展。
- 混合召回和上下文裁剪。

验收：选择接口后能够稳定检索到 Controller、DTO、Service 和异常代码。

### M4：Agent 分析和用例生成

- LangGraph 状态和 Checkpoint。
- 受控 Tool。
- 业务规则提取。
- 测试场景和结构化用例生成。
- 人工审核中断点。

验收：生成四类用例，每条用例有规则和源码来源；服务重启后任务状态可恢复或明确失败。

### M5：执行器与断言

- HTTP Runner。
- 变量替换。
- 认证。
- 断言引擎。
- 请求响应快照和脱敏。
- 单用例与批量执行。

验收：测试通过和失败均能准确记录，AI 不参与 Pass/Fail 判断。

### M6：流程与报告

- 顺序流程。
- 变量提取和下游注入。
- 失败停止或继续。
- 报告、cURL 和失败重试。
- 失败诊断 Agent。

验收：完成“登录 → 创建 → 查询 → 修改 → 删除”的完整回归链路。

---

## 17. MVP 验收用例

### AC-001 正常接口

选择一个带 `@NotBlank`、`@Size` 和唯一性判断的创建接口，系统生成至少：

- 一条正常创建用例。
- 必填字段为空用例。
- 最小和最大长度用例。
- 超过最大长度用例。
- 重复数据用例。
- 未登录用例。

每条用例能够指向对应 DTO 或 Service 规则来源。

### AC-002 关联流程

执行：

```text
登录 → 提取 token
创建记录 → 提取 id
查询记录 → 断言字段
删除记录
再次查询 → 断言不存在
```

运行记录必须保存所有步骤、变量、断言和错误。

### AC-003 错误隔离

批量执行中一个接口超时，系统标记该用例为网络错误；当任务配置为继续执行时，后续用例仍能执行。

### AC-004 安全

- 报告中 Token 和密码被脱敏。
- Agent 无法读取项目根目录外文件。
- 未登记 Host 的请求被拒绝。
- 生产环境无法执行。

### AC-005 可追溯

从报告能够追溯到：

```text
执行版本
用例版本
业务规则
源码符号
扫描版本
Agent 模型和 Prompt 版本
```

---

## 18. 后续版本候选

完成 MVP 并验证稳定后，再评估：

- Git 仓库和分支集成。
- 增量扫描和代码 Diff 影响分析。
- 数据库只读校验。
- 消息队列和异步事件校验。
- Swagger 自动同步。
- CI/CD 定时回归。
- 更多源码语言。
- UI 自动化。
- 团队评审和测试基线。
- 用例自修复，但必须保留人工审批。

---

## 19. 产品底线

以下原则不能为了“更智能”而被破坏：

1. 测试只面向明确授权的测试环境。
2. AI 结论必须可追溯、可审核。
3. 最终执行和断言必须确定性。
4. 密钥和源码访问必须受控。
5. 未经审核的 AI 草稿不能自动执行。
6. 失败必须保留可复现证据。
7. 平台不能修改被测项目的业务代码。

